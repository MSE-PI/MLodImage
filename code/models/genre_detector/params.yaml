train:
  batch_size: 32
  init_lr: 0.0001
  max_epochs: 2
  val_split: 0.2
  nb_workers: 10 # set to 0 if you get an error

evaluate:
  batch_size: 1
  nb_workers: 10 # set to 0 if you get an error

audio:
  audio_duration: 30000
  sample_rate: 44100
  nb_channels: 2
  time_shift: 0.2 # in percentage of audio_duration
